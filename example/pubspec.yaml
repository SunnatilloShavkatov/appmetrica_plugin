name: appmetrica_plugin_example
description: Demonstrates how to use the AppMetrica Flutter Plugin.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  appmetrica_plugin:
    path: ../
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^3.0.2
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
